__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/

# Internal working documents: measurement runbooks and the superpowers
# spec/plan pair. Kept on disk, deliberately not published.
docs/runbooks/
docs/superpowers/

# torch.utils.cpp_extension JIT output (normally ~/.cache/torch_extensions,
# but TORCH_EXTENSIONS_DIR can redirect it here)
build/
*.so

# Profiler output pulled back from the box. These are large and binary; keep
# the number they produced in a doc, not the report itself.
*.ncu-rep
*.nsys-rep
*.sqlite

# Staged weights, if anyone ever points the loader at a local path.
models/
